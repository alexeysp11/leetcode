namespace Studying.Leetcode.Arrays
{
    /// <summary>
    /// Given an array of n integers, construct a product array of the same size, 
    /// such that the i-th element of the product array equals to the product of 
    /// all of the elements of the original array, except for the i-th element
    /// </summary>
    public class ProductOfArrayExceptSelf : Studying.Leetcode.ILeetcodeProblem
    {
        public void Execute()
        {
            System.Console.WriteLine("Product of array except self\n".ToUpper()); 
        }
    }
}
